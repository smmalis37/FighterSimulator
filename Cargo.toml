[package]
name = "fighter_simulator"
edition = "2024"

[dependencies]
enum-map = { version = "2.4", features = ["serde"] }
rand = "0.10"
rayon = "1.12"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
static_init = "1.0"

[profile.release]
lto = true
codegen-units = 1
